# 安全评测

`test_safety_evals.py` 固化了 MVP 的对抗性验收用例：缺失意图、SSH 失败、GPU 占用、
CUDA 冲突、来源质量冲突、陈旧注册表状态、推理 OOM、未测试经验、许可门禁、模型/核心分离、
密钥卫生、避免过度架构、动作标签绕过、跨控制器锁、事故记录和第二台主机可移植性。共有 18 个
命名攻击主题，并由针对性的单元测试和契约测试补充。

这些评测仅使用内存夹具，绝不打开 SSH 连接或调用外部 API。
