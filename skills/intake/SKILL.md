# 需求接收

先把用户每一轮提供的结构化信息递归合并进 `IntakeDraft`。保留之前已经确认、但本轮没有重复提到的值。用白话向用户展示“已记住、现在可以做什么、真正还缺什么”，不要把内部枚举直接抛给用户。

门禁分层执行：只读主机侦察只要求主机选择器、SSH 用户名和端口，并继续强制严格 `known_hosts` 校验；完整 `DeploymentRequest` 门禁在规划前执行；远程写入仍要求许可通过、完整计划和批准的 `READY` 审核。端口策略、框架委托等偏好必须在最终请求和计划中解析为精确值，不能成为执行默认值。
